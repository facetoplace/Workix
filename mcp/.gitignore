node_modules/
data/
profile.md
.env
telegram-channels.json
*.log
# dist/ is published in the public GitHub repo for clone-and-run;
# the server workspace still ignores it via root .gitignore (mcp/dist/).
