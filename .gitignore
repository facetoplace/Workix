# Local / build artifacts
node_modules/
mcp/data/
kwork-api-storage/
.env
.env.*
!.env.example
*.log
.DS_Store
Thumbs.db

# mcp/dist is published on purpose (clone → run without local tsc)

# Never publish secrets / publisher tooling
mcp/key.pem
mcp/*.hex
mcp/mcp-publisher.exe
mcp/registry.local.json
assets/indexnow-key.txt

